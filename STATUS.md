# Release status

Current status: **Candidate — tutorial source complete, awaiting clean-runtime execution**. The pipeline package, offline unit tests, one executed CPU smoke load and generation from the pinned snapshot, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `TASK-INFERENCE` tutorial `tutorials/t5_small_text2text_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 1.1 §3.6) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). Unit tests and static conformance checks are expected to run in GitHub Actions. Nothing here is clean-runtime notebook evidence; the repository is not release-grade until such evidence for the exact candidate commit is recorded in `docs/release-verification.md`.

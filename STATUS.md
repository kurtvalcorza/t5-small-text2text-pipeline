# Release status

Current status: **Candidate / source-complete** — card pass only, no tutorial yet. The pipeline package, offline unit tests, one executed CPU smoke load and generation from the pinned snapshot, and `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1) exist. Unit tests and static conformance checks are expected to run in GitHub Actions once a workflow is added. No `NOTEBOOK_SPEC` 1.0 tutorial notebook ships; the repository remains non-release-grade until one exists and clean-runtime execution evidence for the exact candidate commit is recorded.
